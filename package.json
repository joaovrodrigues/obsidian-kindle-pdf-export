{
  "name": "kindle-pdf-export",
  "version": "1.2.0",
  "description": "Convert markdown to PDF and send to Kindle — no server required",
  "main": "main.js",
  "scripts": {
    "dev": "node esbuild.config.mjs",
    "build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production"
  },
  "keywords": [],
  "license": "MIT",
  "dependencies": {
    "marked": "^15.0.7",
    "nodemailer": "^6.9.16"
  },
  "devDependencies": {
    "@types/node": "^18.19.0",
    "@types/nodemailer": "^6.4.17",
    "builtin-modules": "^3.3.0",
    "esbuild": "^0.24.2",
    "obsidian": "latest",
    "typescript": "^5.7.0"
  }
}
